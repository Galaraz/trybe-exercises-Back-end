const ping = require('./ping');
const users = require('./users');
const products = require('./products');
const selloffs = require('./selloffs');

module.exports = {
  ping,
  users,
  products,
  selloffs,
};